# @package _global_

pipeline:
  runner: luigi
  parallel: false
  workers: 1
  scheduler: local
  log_level: INFO

monitoring:
  enabled: true
  log_dir: outputs/pipeline_logs

notifications:
  enabled: true
  no_notifications: false
